name: Ansible lint
on: push
jobs:
  build:
    name: Ansible lint
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/checkout@v4
    - name: run lint
      uses: ansible/ansible-lint@main


#проверка линтом